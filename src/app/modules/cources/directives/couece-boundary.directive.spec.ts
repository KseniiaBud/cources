import { CoueceBoundaryDirective } from './couece-boundary.directive';

describe('CoueceBoundaryDirective', () => {
  it('should create an instance', () => {
    const directive = new CoueceBoundaryDirective();
    expect(directive).toBeTruthy();
  });
});
